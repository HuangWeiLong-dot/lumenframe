node_modules/
dist/
# `.env` 只匹配这个文件名本身，不匹配 .env.local / .env.production
.env
.env.local
.env.*.local
tmdb_test.json
server/.cache/
# 在线播放源清单：含盗版采集站地址，不进公共仓库；同时因为被忽略，
# 它能扛住部署脚本的 git reset --hard（见 CLAUDE.md 的部署说明）。
# 样例见 server/sources.example.json。
server/sources.json
# 4kvms( nb 源 )的签名 wasm：第三方站点的产物，**进版本控制**（2026-09-20 改的）。
# 理由是部署脚本走 git reset --hard —— 只有提交了，它才会被送到线上那台；
# 留在仓库外面就得每上一台新机器手工拷一次，而漏拷的表现是 kind:"nb" 被**静默跳过**
# （看上去像「4kvms 那条不见了」或「代码没上去」，不报错，很难查）。
# 与 sources.json 不同：那份是配置数据（含采集站地址），仍然不进仓库。
# 来源、为什么必须跑它而不能自己实现签名、以及缺失时的降级行为，见同目录 README.md。

# Torrent-Api-py 是从上游 clone 来**只作参照**的独立 checkout —— 里面的
# routers/ torrents/ helper/ constants/ 已逐字 vendor 进 filmgrab-service/
# （要改就改那边那份，别改这里；没有任何构建或运行路径 import 它）。
#
# 它以 gitlink（索引模式 160000）的形式残留在索引里，指向 1a302617，
# 却**没有对应的 .gitmodules** —— 那个 commit 在父仓库里根本不存在。
# 于是任何执行 git submodule update --init --recursive 的克隆都会直接失败：
#   fatal: no submodule mapping found in .gitmodules for path 'Torrent-Api-py'
# 2026-09-20 Cloudflare 的构建就是这么挂掉的（日志末行：
# "Failed: error occurred while updating repository submodules"）。
# 注意 GitHub Actions 的 actions/checkout 默认**不**递归子模块，所以走 Pages
# 那阵子一直没暴露 —— 这条 ignore 是为了任何会递归克隆的场景，不只为某一家托管。
# 修法是把它从索引里摘掉，本地这份 checkout 原样留着作参照。
#
# 这条 ignore 是**载重**的，不是顺手加的：该目录里带着自己的 .git，不忽略的话
# 下一次 git add -A 会把它重新识别成 gitlink 加回去 —— 等于没修。
Torrent-Api-py/

docs/
.claude/

