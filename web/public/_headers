# Cloudflare Pages 响应头。语法：一行路径（支持 * 通配），随后缩进一行或多行 "Name: value"。
# 放在 web/public/ 下，Vite 会原样拷进 web/dist/，即部署产物的根目录。

# /assets/* 是 Vite 的构建产物，文件名带内容哈希 —— 内容一变文件名就变，
# 所以可以放心 immutable 一年，省掉每次进站对每个 JS/CSS 的一次 304 往返。
# Cloudflare 对这些文件的默认值是 must-revalidate，也就是每次都要回源问一遍。
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# 根路径的 index.html / 404.html 故意**不**加这条：它们的文件名里没有哈希，
# 必须每次回源校验，否则发新版后浏览器会拿着旧的 index.html
# 去请求已经被删掉的哈希资源（表现为整站白屏 + Failed to load module script）。
