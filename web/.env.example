# 复制为 .env.local 后按实际部署填写（本地开发全部留空即可）
#
# Node 后端地址（自建 Linux 服务器或 Vercel 均可），形如 https://api.example.com
# 留空 = 同源相对路径 /api（本地由 Vite 代理，自建整站由 Nginx 反代）。
VITE_API_BASE=

# FilmGrab 剧照服务的对外挂载前缀，与 Nginx 反代方式对应：
#   同域路径反代（推荐）：https://api.example.com/filmgrab
#   独立子域直挂 /api：  https://fg.example.com/api
# 留空 = 不启用，Film Stills 区块自动隐藏（公网部署请注意 FilmGrab 不接受爬取）。
VITE_FILMGRAB_BASE=

# 静态资源子路径：GitHub Pages 项目站点由 CI 自动注入 /<仓库名>/，无需在此填写。
# 用户/组织主页仓库（<user>.github.io）或自定义域名时在 CI 变量 BASE_PATH 中设为 /。
