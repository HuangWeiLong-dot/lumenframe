# 复制为 .env.local 后按实际部署填写（本地开发全部留空即可）
#
# Node 后端地址（自建 Linux 服务器或 Vercel 均可），形如 https://api.example.com
# 留空 = 同源相对路径 /api（本地由 Vite 代理，自建整站由 Nginx 反代）。
VITE_API_BASE=

# FilmGrab 剧照服务的对外挂载前缀，与 Nginx 反代方式对应：
#   同域路径反代（推荐）：https://api.example.com/filmgrab
#   独立子域直挂 /api：  https://fg.example.com/api
# 留空 = 不启用，Film Stills 区块自动隐藏（公网部署请注意 FilmGrab 不接受爬取）。
VITE_FILMGRAB_BASE=

# 静态资源子路径：前端挂在顶点域名（Cloudflare Pages + 自定义域名），
# 保持默认 '/'，无需在此填写，CI 里也不必建 BASE_PATH 变量。
# 只有退回 /<仓库名>/ 项目站点时才需要在 CI 变量 BASE_PATH 里覆盖。

# ---- 可选：Supabase 云端同步（账号 + 片库多设备同步）----
# 两个都留空 = 不启用：不出现账号入口、不发起任何请求、不加载 SDK，
# 观影库仍按原样只存本地 localStorage。
#
# 只能填 publishable（anon）key —— 它是设计给客户端公开使用的，安全性由数据库 RLS 保证。
# service_role / secret key 绝不可进入前端：那等于把整库读写权限发给任何访客。
#
# 建表语句见仓库根目录 supabase/schema.sql（需在 Supabase SQL Editor 手动执行一次）。
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# ---- 可选：在线播放口令 ----
# 留空 = 没有这道门，在线播放照旧直接展开。
# 填了之后中文界面的「在线播放」区块先给一张锁定卡，点开弹窗输入口令才展开，
# 解锁状态记在 localStorage['lumenframe:play-unlock']；弹窗里带管理员邮箱。
#
# ⚠️ 这是**软门**：口令是构建期字面量、编进 JS bundle 的，任何打开页面的人都能从
# 源码里读出来，/api/sources/* 也照样能直接请求。它挡的是随手点进来的人，
# 不是访问控制。真正的门只有一条：别公开部署「在线播放」（见 CLAUDE.md）。
VITE_PLAY_PASSWORD=
